#include <iostream>
#include <cstring>

// TODO: 定义 math 命名空间
// 包含 max(int, int) — 返回较大值

// TODO: 定义 text 命名空间
// 包含 max(const char*, const char*) — 返回较长的字符串
// 提示：使用 std::strlen 比较长度

// TODO: 定义全局 max(int, int) — 返回两个数之和（故意用不同语义）

int main() {
    // TODO: 使用完全限定名调用三个 max 并打印结果
    // math::max(3, 5)
    // text::max("hello", "hi")
    // ::max(3, 5)

    // TODO: 使用 using 声明引入 math::max
    // 然后调用 max(10, 20)，观察结果
    // 再用 ::max(10, 20) 调用全局版本

    std::cout << "请完成以上 TODO\n";
    return 0;
}
