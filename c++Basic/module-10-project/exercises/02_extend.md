# 任务: 增加撤销历史

## 需求背景

用户误操作后需要撤销。

## 需求描述

1. 实现 `undo` 命令改为撤销最近一次操作（不限于 markDone）
2. 支持 `redo` 恢复被撤销的操作
3. 只保留最近 20 次操作历史

提示: 用 Command 模式 + 栈保存操作历史。

## 验收标准

- [ ] undo 能撤销 add/done/remove
- [ ] redo 能恢复被撤销的操作
- [ ] 历史超过 20 条时最旧的自动丢弃
